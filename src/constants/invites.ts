type InviteStatus = {
    accepted: string,
    rejected: string,
    pending: string,
}

export const inviteStatus: InviteStatus = {
    accepted: 'accepted',
    rejected: 'rejected',
    pending: 'pending',
}