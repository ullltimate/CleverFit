export type CardInfo = {
    title: string,
    btnText: string,
    btnIcon: React.ReactNode
}

export type ValuesSignupForm = {
    email: string,
    password: string,
    repeatPassword?: string
}
